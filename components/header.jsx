var Header = React.createClass({
    render: function() {

        // return inly one
        return (
            <div>
                <span>Hearder from REACT</span>
            </div>
        )
    }
});

module.exports = Header;
